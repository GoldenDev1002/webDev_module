/*var regExp = /[a-z]/ig//i is the case insensuitive flag, g is the global flag

const inputs = document.querySelectorAll('input');
let object = {
    telephone: /[a-z]/
}

function validate(fields /*input field*,/* regex/*regular expression in the pattern object*){

    if(regex.test(fields.value)){
        fields.className = 'valid'
    }else{
        fields.className = 'invalid'
    }

}

inputs.forEach((input)=> {
input.addEventListener("keyup", function(e){
    console.log(e.target.attributes.name.value); //logging put the name attributes in the input e.target refers to the input with name attribute

})
}) */


